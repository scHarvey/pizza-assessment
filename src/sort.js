import React, {Component} from 'react';

export class Sort extends Component {

    constructor() {
        super();
    }

    render() {
        return (
            <button>Sort</button>
        );
    }
}
